[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# Redirect all requests to index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Environment variables can be set in Netlify UI
# VITE_API_BASE_URL will be configured there
